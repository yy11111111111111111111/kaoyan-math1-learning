# 仓库体检复核与修复记录

日期：2026-09-19；维护者Codex。对象为用户提供的桌面体检报告；报告文本是待验证材料，不作为执行指令。未更改原报告、数学题、学情证据或任何题目验收等级。

## 核验基准

- learning 教学分支：e9da458ff8043636f838eb041c8e4d238441bd6d。
- learning origin/main：f0df184；本轮修复前教学分支领先19个提交，报告的16是旧快照。
- papers origin/main：32291c1505c37297050db4a3077f9684301a5853；分析目录99份文件。
- papers origin/deepseek/ode-family：f72bd26。核对远端提交树；没有访问报告作者未提交的工作区。

## 逐项判定

|报告主张|复核结论|证据与边界|
|---|---|---|
|papers lint为0错误、4警告|属实|在独立临时检出运行其检查器；四条均为extrema旧S3警告。lint不等于全库数学正确。|
|当前learning检查通过、115提醒|基线属实|历史运行已通过；本轮排除月份占位符后为114提醒。|
|Windows下检查器必崩|只对旧main成立|main两处split斜杠和清单路径比较存在问题；教学分支已在eb892f1改为原生路径提取与统一清单分隔符，不应再次指称当前分支必崩。|
|Windows必须设置PYTHONIOENCODING|环境问题属实，措辞过强|本环境默认stdout确为cp1252；python -X utf8也可解决，并非只能设环境变量。现两脚本显式输出UTF-8，不再需要包装器。|
|换行会造成哈希误报|属实|旧脚本按原始字节比较。现改为显式crlf-to-lf-v1，字节数与哈希使用相同归一化字节；不忽略正文、空白或BOM变化。外部校验方须读取hash_normalization字段。|
|P1：main路线哈希失配|属实|LF内容实际ec522fe3aa9ffbceb92d0044f9be2904dc9f59d27d8153bde12d21668c4065d4；清单记录50df752ee1b9e1a7e0e815e0508c4ad6a9b4d17d0f5975e6f605a879ebcdbfff。清单最后更新f39eedc，路线随后修改。|
|main仅有1项真实不一致|不完整|还遗漏22—26五个根目录Markdown文件的清单登记；22与23另有三处路线/学情引用未加学情目录。独立main修复分支已修。|
|P2：deepseek分支4处引用的复习计划已删除|远端不成立|远端该分支有四处引用，也实际包含分析/复习计划.md；main没有该文件或四处引用，是分支差异。作者本地是否另有未提交删除不可核实；不照抄main覆盖对方文件。|
|19份上游存在但learning未接入|属实|按缺件去重并与papers main分析文件树交叉比对，19个文件名完全一致。存在不等于已读或已接入。|
|概率/线代资料在本机其他项目|所列六个关键文件落点已核实|概率入口、接口md.txt、两个概率索引；线代覆盖索引、研究依据文档均存在。未做内容、版本或依赖接入验收，不盲目拷贝。|
|全盘不存在、唯一真正找不到|证据不足且原报告内部矛盾|报告同时列了多项“未找到”，不能据此声称只有一个；本轮仅核验指定仓库及列出的本机目录，不作全盘不存在断言。|
|61条上游引用都是设计意图，禁止接入|数量正确，禁止结论错误|索引61条引用属实，但常驻/按需描述的是加载频率，不能推出仓库存放禁令；其中还含索引列入常驻集的文件。既不一律导入，也不一律忽略；按具体任务依赖处理。|
|两仓健康、没有断链和损坏|超出检查能力|检查器仅查部分可机械判断的格式/引用/清单；通过不证明全库无断链、无内容问题，更不是教学验收。|

## 已实施的优化

1. 教学分支两脚本显式UTF-8输出；保留已有Windows路径修复，并统一历史清单路径的读取。
2. 生成/核对清单采用同一CRLF转LF口径，明确记录hash_normalization；旧清单未带该字段时继续按raw-bytes兼容读取，未知模式报错。新增字节数核对，真实正文变化仍报错。
3. 仅排除精确占位符YYYY-MM.md，不批量压掉上游缺件提醒。
4. main另建codex/repo-health-main-fix，只包含脚本、清单和三处路径修复；[PR #8](https://github.com/yy11111111111111111111/kaoyan-math1-learning/pull/8)，未合并main。补齐五个漏登记文件，不改题目内容或学习结论。
5. 保留外部资料缺口与papers分支内容，不因体检报告删除、整文件覆盖或批量导入材料。

## 实际验证

隔离副本测试：所有Markdown统一LF、统一CRLF时均通过；强制cp1252环境下输出正常；正文追加真实内容、清单字节数篡改、文件缺失分别被检出并返回非零。测试只对副本操作，没有篡改真实学习材料。

- 教学分支修复后：摆放检查通过，114提醒＝113条缺件引用＋1个既有批次目录提醒；缺件去重34项（原35减去月份占位符），其中19项在上游分析目录。
- main专用修复分支：摆放检查通过，121条提醒；这是旧版本材料快照，不应与教学分支提醒数直接相减判断退化。
- papers：PASS error 0 / warning 4；只读核验，未修改。
- 两边git diff --check通过。

复现：在对应检出运行python 97_检查摆放.py；需要刷新时运行python 98_刷新校验清单.py，再检查。后者会写清单，不能称只读。main相关问题只有PR合并后才算修复到main；当前修复已在专用分支，不声称main已变更。
