# 跨AI任务与反馈交接

本文件是填写式模板，配套Generation/Validation 1.1.0，不含候选数学题、答案或学生表现。按本次任务只选一节，不把整份模板传给盲验者。

## 1. 给出题AI的任务

> 按当前工作包的启动入口和生成协议，读取20路线当前节点与30中的实际状态。只为当前一个核心目标准备候选题；不要一次生成整条路线。先明确目标claim、允许前置和所需过程证据，再形成题目。日常初诊不自动使用K-DIAG。已确认缺新工具则先补最小教学，未知状态不写成已掌握。
>
> 题面短、条件完整；难度来自条件控制、表示转换或方法选择，不堆计算。首轮不提示指定方法；允许合法不同路线，对未展示能力不认领证据。候选未经独立验收及Fit不得正式投放。
>
> 用户若只要求候选包，交付后停止；若已授权教学，则按协议完成验证、Fit和默认一次一题投放，不每题重复询问授权。明确要求复习题组时可整组交付，逐题验收并审计真实题序的相互提示，不一次生成整条路线。

内部先填写：route版本、node_id、target_action、target_claim、P/C/A、实际状态引用、planned_evidence、覆盖限制、若为迁移则写transfer_change。仅填写本题实际需要。

## 2. 给独立Validator的Stage A

仅发送现行Validation允许的字段：

```yaml
stage_A_packet:
  item_package:
    item_id: 待填
    package_version: 待填
    title: 待填
    stem: 待填
    options: []
    instructions: 待填
    response_requirement: 结论与关键依据
    scoring_rule:
      fully_acceptable: 待填完整评价准则，不含答案键或未公开的指定方法
      partially_acceptable: 待填部分响应，例如只有正确数值
      unacceptable: 待填
    source_refs: 待填
  prerequisite_profile: 已注册ID
  required_prerequisites: 明确陈述及精确来源
  convention_bindings: 已注册约定及绑定
  allowed_tools: 待填
  declaration_set_version: MATH-ITEM-DECL-v1.0.0
```

不要附路线、用户背景、目标claim、答案、生成rationale、错误模型、投放背景或原聊天。题目/评分变化须按版本和依赖重验，不在Stage B偷偷改题。

检查字段内容及引用材料，不只查字段名；来源只读实际需要的定义与约定。评分准则不能夹带生成者答案集合，必要评分条件也不能为盲验而删除。若已暴露，按Validation记录受影响独立检查，Light也不能用生成者复算代替。

## 3. 封存Stage A之后的材料

收到含精确版本、独立答案和实际核验产物的Stage A封存记录后，才发送：

```yaml
sealed_stage_B_packet:
  generator_solution: ...
  generator_answer_set: ...
  generator_rationale: ...
  target_claim: K-...
  diagnostic_partition: 不适用时为none
  generation_self_audit: ...
  suggested_validation_level: Light | Full
  presentation_evidence:
    presentation_version: ...
    visible_context_excerpt: 实际可见的相邻讲解、标题与提示
    visible_item_order: []
    prior_items_or_solutions_visible: []
    coverage: complete_within_declared_scope | incomplete | unknown
```

用于泄露审计的实际标题/提示/前文只在封存后提供；保留决定性方法线索本身，不为通过审计删除。剔除学生能力、自述、错误记录与学习状态；这些属于Fit侧，不属于Validator判断。

未取得的范围标为未覆盖，不假定没有提示。标题外的提示、前题解答或题序变化时，核对报告中的`presentation_evidence_ref`，重验受影响的leakage和claim，不因为题干未变就直接沿用。

不能创建真正独立上下文时，报告缺少的必需步骤，交付待验收材料。不伪造Full、不把有限搜索没发现改写成不存在。

## 4. 给教学执行者的Fit材料

只交付已验证Task Fingerprint、题面版本、实际状态、assignment policy、administration context。Fit按03逐项匹配；不再附生成者自由解释来劝说投放。已知前置缺失与未知分开；不因结构合法判定用户适配。

学习者练习作答前只看题面与作答要求，不额外附解答、教师评分依据、方法提示或下一题变化；用户明确要求完整解答或教师材料时按请求交付，不将该材料之后的表现当作独立发现证据。

## 5. 给路线维护者的作答反馈

每次反馈尽量短，但保留真实证据：

```text
节点与路线版本：
题目ID、版本、来源：
题目及实际作答要求：
是否给过提示、最近是否刚教/刚练：
用户原作答或可访问定位：
核验后的第一独立断点（没有则写无）：
本次实际支持的局部能力：
未被本次测到的能力：
已做的最小修复及用户随后表现：
建议下一动作与理由：
```

不要只传“做对/做错/懂了”；不要补写用户未表达的理由。图片模糊只标不确定。若用户提供反驳题目合法性的具体证据，先按Validation处理，再决定是否可记录学习证据。

## 6. 保存去向

- 纸笔：保留当时思路，不强迫改成正式讲义。
- 正确结构：需入库时读取目标Vault规范，不默认自动写入。
- Error Log：只收有价值且值得长期追踪的学习者错误模式；AI错误、OCR错和连锁后果不登记。
- 项目状态：在30中维护可追溯证据；不再建立第二套私有状态文件。
- 路线：只有行为证据或真实前置依赖变化才调整；不由刷题数或目录进度直接推进。
