# 给接手本仓库的 AI

本仓库是考研数学一的出题与学习基地，收五类内容：出题原则、出题、解析、学习情况、复习记录。

## 先读这三份

1. `AGENTS.md`：**执行规则的唯一出处**，全部条目适用。本文件与它冲突时以它为准。注意本文件可能是会话启动时缓存的旧副本，与 `AGENTS.md` 不一致时先重读 `AGENTS.md`，不要照本文件执行。
2. `README.md`：目录地图，五类内容各从哪个文件开始。
3. `00_先读我_使用说明.md`：本包实际组成、目录结构与按任务的加载路径。

然后按当前动作只加载需要的模块。不要一次读全部题库和答案。

## 目录一眼

```
根目录          入口 00/01/02/03、出题原则（共享声明集、Generation、Validation、92批次规范）、维护 40/90/91/99
题库/<批次>/     学生卷                      可整份交给学习者
审查/<批次>/     审查交接与 Stage A 题包      只给审查方，投放前不给学习者
解析/<批次>/     教师侧参考推导、验收报告      含答案，独立作答前不打开
学情/           复习路线、当前学习状态、作答后衔接
复习记录/        按月的真实作答与复习事件
资料/           覆盖索引、方法族
```

新增批次按 `92_题库批次规范.md`，写复习记录按 `复习记录/00_格式与规则.md`，两者都要在 `99_工作包文件与版本清单.md` 登记。

## 四条硬规则（摘要，正文见 `AGENTS.md`）

1. **可见性按目录定**：`题库/<批次>/` 可整份给学习者；`审查/` 只给审查方；`解析/` 在独立作答前不打开，也不作为下一题提示。放错目录按泄露处理。见 AGENTS 第 13 条。
2. **未验收不等于可投放**：生成侧参考推导不是验收结论；题面修订后不继承旧版本的验收结论；验收通过后仍要按 `学情/` 做适配判断。见 AGENTS 第 6、7、14 条。
3. **只有真实作答才算证据**：不因回答正确、表示理解或聊得久就判定掌握。资料错误、AI 错误与已撤回的诊断不写进学习者记录；已撤回项见 `学情/30_考研数学一_当前学习状态.md` 第 4 节。见 AGENTS 第 3、5、7 条。

4. **分工要用户切换**：本地助手默认不出题、不自行验收；用户明确把当前角色切换为出题者或教学者时才进入该动作。审查与修正交由用户指定的其他 AI。见 AGENTS 第 2、14 条。

投放前先读 `99_工作包文件与版本清单.md` 的批次表确认状态。规则有变只改 `AGENTS.md`，再同步本文件的摘要，不在两处各写一套措辞。
