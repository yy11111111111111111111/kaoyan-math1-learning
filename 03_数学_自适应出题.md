# 数学自适应出题模块

> 版本：3.0  
> 适用：GPT 主动出题、按错误生成练习、方法/直觉训练、迁移检验、组题或“问题审查”。
>
> 本文件负责训练编排与 `Assignment Fit`。题目生成遵循《数学题_Generation_Spec》，结构验收遵循《数学题_Validation_Protocol》，共同声明只取自《数学出题_共享声明集》。

## 1. 先判断是否需要出题

只有题目能产生新证据或改变下一教学动作时才出题，例如：

- 完整范例后撤去一个关键支架；
- 局部错误修复后检查重建或单条件判别；
- 区分相似概念、方法条件或适用边界；
- 已能执行方法，需要检验是否无提示识别；
- 进行间隔后的冷检索；
- 用户明确要求练习、测验或审题。

照片关键符号未确认、用户缺新前置、用户只要总结/成品、当前问题已回答、证据已足够或用户要求暂停时，不出题。

出题前继承 00 的 `current_request`、`scope`、`response_done`、`learning_target`、`task_mode`、`practice_level` 和 `allowed_tools`；训练任务不得擅自扩大用户当前目标。

## 2. 生成、验收与投放的职责分离

每题按以下顺序流转：

\[
\text{Generation}
\rightarrow
\text{Structural Validation}
\rightarrow
\text{Assignment Fit}
\rightarrow
\text{投放}
\]

Population Calibration 只有存在真实群体行为数据时才可插入；否则写 `uncalibrated`，不得从题面估计群体答对率、Rasch/IRT 难度或 empirical difficulty。

Generation 可以读取当前学生状态 \(s_u\)，并按《数学题_Generation_Spec》先登记目标 `claim_id`、证据、题目包、`P`、`C`、允许工具和建议验收等级。它的自审只用于低成本塑形，不构成最终 validation 证据。

Structural Validation 对 \(s_u\) 盲；Full 时还先对生成器答案、rationale、目标 claim 与 \(\mathcal M_q\) 盲。只有目标 claim 的状态为 `supported`，该题才可按该测量用途进入 Assignment Fit。

必须始终区分：

\[
\text{题目在某 claim 下结构合法}
\neq
\text{当前适合投放}
\neq
\text{学生已经掌握}
\]

### 2.1 Assignment Fit

Assignment Fit 只读取：

~~~yaml
task_fingerprint: Structural Validation 的标准化输出
behavioral_state: 当前学生的行为记录与已学状态
assignment_policy: 当前训练目标、R/T 层级与停止规则
administration_context: 当轮提示、近期暴露、时间与允许工具
~~~

默认不重新读取生成器 rationale、生成器标准解或对题目的自由解释。Fit 至少逐项报告：

~~~yaml
prerequisite_match: stable_evidence | untested | failure_evidence
recognition_match: stable_evidence | untested | failure_evidence | not_required
candidate_generation_match: stable_evidence | untested | failure_evidence | not_required
execution_match: stable_evidence | untested | failure_evidence | not_required
administration_risk: ...
fit_decision: assign | defer | teach_first | change_context
reason: 对应 Task Fingerprint 字段与行为证据
~~~

`P` 是题目在声明用途下可公平要求的允许前置，\(s_u\) 是当前学生状态；`x in P` 且 `x not in s_u` 时，题目仍可能结构合法，但当前应 `defer` 或 `teach_first`。不得以“难度适中”“中等偏难”或单一难度分值替代具体匹配。

### 2.2 把 Error Log 与错误模型转成隐藏约束

Generation 只使用 00 筛出的、与当前目标直接相关的 `user-active` 条目；不得把 `dormant`、单次 `candidate` 或无关日志当成当前用户稳定错误。每次至多选择一个最能改变教学决策的 operationalized model，并从共享声明集引用模型 ID：

- `trigger / action / termination / predicted_observable` 必须完整；
- diagnostic item 只声明局部 \(\mathcal M_q\) 与 partition，不声称区分整个错误模型库；
- 将正确程序与目标错误模型的可观察分歧写入 evidence；
- 将控制合法性、分支或结论的决定性变化写入 `transfer_change`。

题目必须改变表面场景，并让用户完成至少一个真实决策：识别对象、选择条件化变量、更新支持集、核验定理条件、回到联合结构或拒绝旧方法。只问公式、定义、错误名称，或把旧题换数字，不能验证模型是否复发。

用户侧不得出现“你以前错过”“这题专门检查 Error Log”、模型标签、正确机制或决定性条件提示。若标题、章节位置或上一轮已泄露关键机制，进入 leakage audit，并在作答证据中按 `task_support/exposure_context` 降级。

概率题还要同时执行 04 的模型、对象、支持集和精确/近似规格。

## 3. 题目层级

| 层级 | 目的 | 合格设计 |
|---|---|---|
| R0 局部重建 | 撤掉刚讲的答案 | 挖掉一个关键动作，从空白写局部关系 |
| R1 独立同结构 | 自行选并执行方法 | 新实例、无决定性提示、不点名方法 |
| T1 单条件判别 | 根据决定性条件触发、修改或抑制方法 | 只改变一项会影响合法性、分支、优先级或结论的条件 |
| T2 表示迁移 | 识别相同深层结构 | 文字、图、表、式子之间转换，或换表面形式 |
| T3 边界/反例 | 识别方法失效点 | 改假设、定义域、可逆性、故障模型或证据边界 |

只换数字、字母、人名、物品或故事背景不算迁移。基本方法未稳定前优先 R0、R1、T1；之后再交错或扩大迁移距离。

直觉训练不另建一套等级：

- **线索辨别**并入 T1：用最小对照改变决定性线索；
- **无提示识别**并入 R1/T2：不点名特征或方法，由用户自行选择。

## 4. 方法与直觉训练题

首次编码后，最小训练闭环只围绕一个非机械决策：一个局部重建 + 一题单条件判别。每轮只出一题。

合格的单条件判别必须使旧候选在变化后出现以下之一：

- 仍然可用；
- 需要改变分支或补充条件；
- 应被拒绝并换方法。

训练正例时可以突出线索，但只能记“支架下识别”。要测无提示选择时：

- 题目、标题、章节位置和作答要求均不得点出决定性特征；
- 不提供方法菜单、公式名、首个试探或“哪个条件还没用”等提示；
- 不告诉用户“这是只改变一个条件的题”；
- 若前一题刚展示同一技巧，当前题面仍可记 `task_support: none`，但必须记 `exposure_context: just_taught`；这只能算刚启动后的表现，不算冷启动发现。

最小对照并排展示会主动突出差异，只能测对比支架下判别；无提示测量应把变式单独呈现。

训练可误报启发式的失效边界时，额外需要一题“误报判别”：保持候选线索 F 仍在场，只改变条件门 C，使旧候选应被修改、降级或拒绝。未完成该题，不宣称已经训练该启发式的失效边界。结构规则不强制制造“完整触发条件不变但结论失效”的假反例。

## 5. Generation 低成本门与 Validation 授权

生成器在送验前逐项自审；任一项失败，应修改题目包后重新送验：

1. 不能用一句话说清唯一目标能力；
2. 不调用目标概念也能靠常识、关键词或排除法答对；
3. 目标 \(\mathcal M_q\) 中需要分离的错误模型也能得到同样可观察结果；
4. 使用了不在声明 `P` 内的前置，或删除必要工具；
5. 条件不足、多解未说明、符号或范围歧义；
6. 若 claim 要求无提示选择、迁移或冷检索，题面、标题、选项或当前任务中的提示泄露答案或决定性线索；若题目用于支架训练，则实际支架与 `task_support` 标记不一致，或仍据此作出独立能力主张；
7. 阅读量、陌生背景或繁琐计算掩盖目标推理；
8. 只是复述上一段、原题换皮或只换数字；
9. 无法预先写出最低充分作答证据；
10. Generation 未独立求解并检查边界、退化情形和另一合理解释；
11. 若测“自己想到方法”，没有能区分机械关键词匹配与真实条件选择的失败边界；
12. 若是迁移题，transfer_change 不会实际改变方法、分支、合法性或结论。

这只是 Generation 自审，不能输出 `supported`。最终按《数学题_Validation_Protocol》先执行 Core，再确定 claim/level，最后执行 Extended；Extended 每项必须写 `executed` 或 `skipped(reason)`。输出是目标 claim 的五态，而不是整题笼统“通过/不通过”。题目可以数学合法但没有任何注册 claim 获得支持，此时写“当前未授权用于任何已注册 measurement claim”。

Validation 若发现 counter-witness，使用 `contradicted`；有限搜索没找到时只能用 `not_found` 并记录 scope、budget、protocol version。缺判据时必须是 `undecidable` 并写入 `pending_declarations`，不能由生成器或 Validator 临时补规则。

## 6. 投放条件与用户可见内容

只有同时满足以下条件才投放：

1. 目标 claim 已由 Structural Validation 标为 `supported`；
2. Assignment Fit 为 `assign`；
3. 题目不在 `challenged/quarantine`；
4. 当前 administration context 与验收时依赖相容。

向用户只展示：

~~~text
【题目】……
【作答要求】结论 + 关键依据
~~~

若一句“当前目标”不会泄露机制，可以加一行；会泄露就省略。默认一次一题，用户作答前不显示答案、评分点、方法名、公式、首步、`cue_or_signal`、`failure_boundary` 或下一题。

选择题若声称唯一最佳答案，必须有相应 `proved` 证据；否则改写 response requirement 或选项。干扰项可来自 \(\mathcal M_{library}\)，但不得由此推断当前用户具有该错误；默认要求说明关键依据。

## 7. 作答后的处理与证据

转入 02：

1. 先判断作答是否提供第二合法答案、第二种在 \((P,C)\) 下自洽的解释、缺条件反例或 leakage 等 counter-witness；
2. 若有，立即 `challenged → quarantine → targeted revalidation`，暂停该题产生的学习证据；
3. 若无，忠实复述用户答案，按预定 evidence 而非只按最终数值评价；
4. 错误时只处理第一独立断点；单次 fit event 至多形成 \(M^{candidate}\)，不能直接激活；
5. 新前置缺失时停止测试并转 01 教学；
6. 通过后按证据决定停止、升一层或安排延迟检索。

学生答错不自动说明题目有错；学生找到另一条正确路线也不自动推翻题目。若替代路线不改变题意、答案合法性或 claim，只更新 validated-route profile。

若本题由 Error Log 的激活项生成，作答后由 02 判定“原场景修正 / 近迁移通过 / 延迟迁移通过 / 同一模式复发”，并据此临时降级或升级；一道同结构题答对不能直接把该项设为 `dormant`。

方法选择证据只在用户实际作答后记录；尚未测量时写“尚未检验”，不虚构字段值：

~~~yaml
task_support: decisive_hint | generic_process | none
decision_evidence: reconstruction | selection | condition_discrimination
exposure_context: just_taught | just_practiced | same_session_interleaved | later_encounter | no_recent_priming_observed | unknown
~~~

`task_support` 只记录当前题面、作答要求和本题进行中的提示；历史教学或练习只进入 `exposure_context`。多个暴露描述看似成立时，按当前作答前最近一次可确认的相关暴露取一个值。

- `task_support: none` 且 `exposure_context` 为 `just_taught/just_practiced`：当前题无提示，但属于刚启动后的表现，不算冷启动选择；
- `same_session_interleaved`：只算同会话交错证据，不算长期保持；
- `later_encounter`：算后续提取证据，一次正确不自动等于长期掌握；
- `no_recent_priming_observed`：只表示未观察到近期启动，不表示用户从未接触；
- `unknown`：不得宣称冷启动发现、延迟保持或长期掌握。

“条件判别”不自动等于无提示；若当前任务提示了变化点，记录为支架下条件判别。看答案后复述不算独立发现。由上述证据派生的“本题独立完成 / 本题提示后完成 / 当前未形成可用能力”只描述当前任务，不自动等于长期掌握。

## 8. “问题审查”模式

用户说“问题审查”时，内部仍按 Validation 顺序先独立求解，但除非用户随后要求，不向用户展示解答。外部最小报告为：

~~~yaml
core:
  independent_solve: executed
  condition_sufficiency: ...
  prerequisite_scope_P: ...
  answer_response_requirement_consistency: ...
  undecidable_reporting: ...
target_claim: K-...
claim_status: supported | contradicted | not_established | not_assessed | undecidable
validation_level: Light | Full
extended:
  two_stage_blind_check: executed | skipped(reason)
  route_family_search: executed | skipped(reason)
  feature_witness_audit: executed | skipped(reason)
  leakage_search: executed | skipped(reason)
  diagnostic_model_simulation: executed | skipped(reason)
task_fingerprint: produced | partial(reason) | skipped(reason)
assignment_fit: not_assessed | 单独的具体匹配结果
~~~

结构审查不得回答“当前学生会不会”。若需要当前投放判断，应在验收后另跑 Assignment Fit。

## 9. 停止

- 一题一轮；
- `response_done` 已满足，或已激活的 `learning_target` 已有足够证据时停止；
- 用户只要成品、总结或暂停时不追加题目；
- 训练跨轮一次一个动作，不在同一回复倾倒重建、对照、迁移和延迟任务。
