# 三科学情连接与流程优化

版本：试行 v0.1（2026-09-16）。这是本次用户授权的工作方法与优化候选记录，不是新执行协议、题目验收或学习者成绩。执行规则仍以AGENTS第16—17条为准，事件字段仍以复习记录/00_格式与规则.md为唯一出处。

## 1. 一个共同底座，各科各自取证

不新建各AI的私人状态副本。真实事件写当月复习记录；当前状态只维护学情/30_考研数学一_当前学习状态.md。30按科目保留当前节点、证据引用、待检项和下一动作，更新线代不覆盖高数；概率未取得记录时明确待接入。

每个教学AI负责本轮科目的首答、提示、干预后实际表现。Codex读这些增量再调整路线与出题。原作答、教学AI摘要、Codex复核分开归属；未取得原文时不能写成已独立核验。人工启动教学AI与交回增量仍是必要动作，仓库不会自动唤醒或同步AI。

## 2. 最小记录循环

1. 开始：获取用户指定分支的最新提交，只读30中当前科目、必要路线和相关事件。
2. 教学：保留首次关键推理及实际提示；原作答与干预后的表现分开。仅讲解而未作答，不能写成独立完成。
3. 结束：按现有格式追加真实事件，再更新30对应科目；原来正确的部分保留。其他科目的节点不因本轮自动变化。
4. 交接：有写入能力则独立分支/PR，无能力则交付待同步增量。增量带基准提交、维护者、题号版本、来源、提示暴露、实际表现与下一动作。用户交给Codex后才做导入，不声称浏览URL等于已写回。
5. 出题：Codex先核对增量与已有记录，再决定巩固、条件变化或跨科任务。不按完成题数自动升级。

一条错误只记录第一独立断点；AI误读与撤回诊断放排除/维护记录，不放学习者错误队列。疲劳、字迹不清、漏答不直接解释为不会。当前原图失效的记录保留来源与边界，后续补图时追加确认，不覆盖当时的不确定性。

## 3. 联系不是共同标签，而是一条需检验的桥

教学AI发现联系时，在下表登记候选：具体源节点、目标节点、可复用关系、各科条件差别、所引用的事件及下一验证动作。只有关键词相同或形式相似，不足以认定迁移。

候选 → 已教学 → 本次独立应用有证据 → 后续保持有证据，均需注明日期与记录ID。状态只描述这条连接已经发生到哪一步，不能改写成两科整体掌握。没有实际跨科作答，就只保留候选；不把计划写进真实复习事件。

跨科验证仍按现有出题/外部审查约定处理。学习者作答前不公开候选连接、方法提示或答案。如果先讲过连接再做题，照实记录近期暴露，不能称自主发现。

| 连接ID | 源节点 → 目标节点 | 可复用关系及必须区分的条件 | 证据与当前状态 | 下一动作 |
|---|---|---|---|---|
| C01 | 线代基/坐标/线性映射 ↔ 高数Jacobian/参数化 | 矩阵表达映射与坐标；一般非线性映射的Jacobian是指定点的一阶局部表示，不能当成全局固定矩阵 | 线代LA-08 v2有基下坐标正确表现；高数现有参数化交接仅作来源；跨科独立应用未检验，候选 | 各自基本调用稳定后，外部审查一题，观察能否说明对象与局部条件 |
| C02 | 线代实对称矩阵/二次型 ↔ 高数Hessian/极值 | 特征值与二次型符号；Hessian正定是满足相应可微条件的驻点判别工具，半正定时不能直接判极小 | 线代对称结构有历史正向摘要；高数退化主项尚待检；连接仅候选 | 高数局部修复稳定后，检查能否辨别正定与退化情形，不能要求重新发明理论 |
| C03 | 线代线性变换/二次型 → 概率随机向量/协方差 | 方差可写二次型；随机向量分量是随机变量，协方差矩阵半正定不保证正定；需相应二阶矩存在 | 本库概率真实学情未接入，候选待前置，不投放 | 先接收概率已有记录，再判断是否需要最小教学与验证 |

上表是教师侧计划。不得把它整份给学习者作为下一道无提示题的解前提示。三科关系不必一次全接；先选择当前任务中有用的一条桥。

## 4. 流程优化候选（保留，不自动实施）

每次发现流程问题，在下表追加简短条目：实际问题、建议、试用方式、观察证据、决定。学习者原作答仍保留在月度事件，不复制到此处。规则冲突按AGENTS处理；改变共同执行约定前交用户裁定，不将建议悄悄升为硬规则。

| ID/日期 | 问题或动机 | 优化建议 | 怎样试用、看什么 | 当前处置 |
|---|---|---|---|---|
| O01/2026-09-16 | 口头失误曾盖过多次正确证明，AI诊断被用户撤回 | 负向诊断前同时引用已有正向证据，并保留用户澄清 | 下一轮判读检查是否忠实定位首断点，若仍需用户纠正记录原因 | 待试用，不构建稳定错误模型 |
| O02/2026-09-16 | 临时照片路径已失效，跨AI不能重看原图 | 优先在获授权且原图可用时保存到统一证据位置，记录来源；失效时明确待补 | 下一次照片提交看是否可追溯、是否需要重传；不自动外发或同步无关文件 | 本轮原图待补，保存流程候选 |
| O03/2026-09-16 | 长记录增加维护和阅读负担 | 日常只写能改变下一动作的证据，同题的修正用新事件引用旧记录，不反复复述全部历史 | 试用一轮，观察接手AI能否从30和记录ID恢复、是否重复询问历史 | 待试用；不删必要字段 |
| O04/2026-09-16 | 各AI并发改30容易覆盖他科进度 | 顺序交接，独立分支/PR，只修改对应科目段落；冲突逐条保留 | 后续查看是否丢失他科证据，自动清单冲突重新生成 | 沿现有规则执行，待实用反馈 |
| O05/2026-09-16 | 固定跨科频率可能增加疲劳，掩盖基础断点 | 每次交接只推荐一个有用连接或明确暂缓，不固定按周凑题 | 对照作答与实际负担：连接是否改变解释或选法，原断点是否复发 | 待试用，跨科任务未实施 |

优化复盘由真实困难触发，或由用户在若干回合后请求；无需每轮开一次流程会议。采用、保留或放弃建议时追加日期、证据引用与理由；旧意见不删除。运行脚本通过只说明摆放/清单检查，不说明教学流程有效。

## 5. 教学AI结束时的简短交接

不增加第二套事件字段。完成现有记录后，只补三行总结供Codex定位：

> 本科更新：记录ID、当前一个待检点与下一动作。
> 跨科联系：连接ID；本轮未涉及则写无；没有实际迁移证据就不升级状态。
> 流程反馈：优化ID及新观察；没有问题就写无。

本稿形成基准：cf43b0c；用户本轮授权设计并保留可优化的方法。以上连接和流程优化尚未经过实际回合验证，不宣称有效性已验收。
