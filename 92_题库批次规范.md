# 题库批次规范

2026-09-11 建立，同日按内部审查修订。本文件规定一个题目批次由什么组成、放在哪、怎么命名、状态怎么写。新增批次照此执行，不另立格式。

可见性规则的正文在 `AGENTS.md` 第 13 条，本文件只在需要时引用，不另写措辞。

## 一个批次的组成

同一批次在三个目录下用同一个目录名，按可见性分开存放。

| 文件 | 位置 | 给谁 | 说明 |
|---|---|---|---|
| 学生卷 | `题库/<批次>/` | 学习者 | 只含题面与公共作答要求。不含答案、评分依据、方法菜单或下一题的迁移变化 |
| 审查交接与 Stage A | `审查/<批次>/` | 审查方 | 审查指令、阅读顺序、逐题 Stage A packet。含评分准则与前置声明，可能点名决定性方法，因此不进 `题库/` |
| 教师侧参考 | `解析/<批次>/` | 教师侧 | 生成侧参考推导或参考答案、逐题设计目的、证据边界 |
| 验收报告 | `解析/<批次>/` | 教师侧 | 独立审题或协议验收的结论、必要修改、投放建议 |

Stage A packet 的字段与禁止项按 `数学题_Validation_Protocol.md` §2.1：不得含答案键、target claim、生成理由或学习者状态。

## 命名

- 批次目录：`<代号>_<内容>`，例如 `MV26_多元微积分水平检测`、`LA_空间到方程_10题`。代号一旦用于 item_id 就不再改。
- 批次内的文件用描述性名字即可，**新批次不必再带全局编号**。现有两批的 `21/22/32/33/35` 编号只为兼容既有引用而保留。
- item_id 形如 `MV26-01`，与学生卷题号一一对应。题面改动升 `package_version`，不改 item_id。
- 文件改名等于改路径，须同步全包引用与 `校验清单.json`。

## 版本与状态

- 学生卷、Stage A 题包、教师侧参考三处的题面必须逐字一致。改题面时三处同时改，并在验收报告留变更记录。
- 卷版本写在学生卷开头（如 `MV26 v2`）。**修订后的版本不继承旧版本的任何验收结论**，也不得表述成旧版本原本已通过。
- 状态词固定用：`generation_complete`、`independent_review_done`、`protocol_validation_incomplete`、`assignment_fit_pending`，以及 claim 五态 `supported / contradicted / not_established / not_assessed / undecidable`。
- 每个批次在 `99_工作包文件与版本清单.md` 的批次表里占一行，写清当前版本与状态；风险与遗留限制写进 `90_考研数学一_质量风险与维护记录.md`。批次表是状态的唯一出处，其他文件只指向它。

## 出题与验收的分工

生成与验收由不同方执行。生成侧参考推导不是验收结论；同一上下文里先看过答案再复算，不能记为独立验证。Stage A 封存后才解封 Stage B，顺序与字段按 `数学题_Validation_Protocol.md`。

验收完成也不等于可以投放：投放前还要按当前学情做 Assignment Fit，依据在 `学情/`。

## 新增一个批次的最小动作

1. 定代号，在三个目录下建同名批次目录（没有的那类文件可以暂缺，不建空文件）。
2. 写学生卷与 Stage A，按本文件与 `数学题_Generation_Spec.md` 执行。
3. 交外部审查，回来的结论与教师侧参考放 `解析/<批次>/`。
4. 在 `99` 的批次表加一行，遗留限制写进 `90`，文件哈希更新进 `校验清单.json`。
