一、MV0916 最新真实作答与教学结果
================================

用户已完成 MV0916 12 题学生卷。以下是本轮真实证据，请不要把“刚讲懂”直接写成稳定掌握。

A1：
- 主链正确：
  G(γ(t))=c → 链式法则 → ∇G(P)·v=0；
- 能解释非零梯度给出法向；
- 漏答“若 ∇G(P)=0，正交等式本身不足以给出法向方向”。
- 属于答案不完整，不是核心结构错误。

A2：
- 从可微定义写出
  r(u+Δu,v+Δv)-r(u,v)
  =Dr(u,v)(Δu,Δv)^T+o(...)
  =r_uΔu+r_vΔv+o(...).
- 理解 Dr 两列分别是参数基方向经局部线性映射后的像。
- 能推出
  dS=||r_u×r_v||du dv.
- 已理解这是局部一阶近似，不是有限曲面片精确等于平行四边形。
- 这是正向证据。

A3：
- 用户原思路方向正确：想把曲面投影到 yz 平面并和向量面积第一分量联系。
- 第一实质断点：对投影映射
  (u,v)→(y,z)
  误用了三维叉乘式面积公式。
- 已教学并理解：
  R^2→R^2 的局部面积伸缩用 Jacobian determinant，
  \[
  \frac{\partial(y,z)}{\partial(u,v)}
  =
  (r_u\times r_v)_x
  \]
  从而
  \[
  dy\,dz=n_x\,dS
  \]
  无向投影取绝对值。
- 这应作为后续训练重点之一：区分
  R^2→R^3 用叉乘，
  R^2→R^2 用 Jacobian 行列式。

A4：
- 用户原先知道“Stokes 来自 Green”与“内部边界抵消”，但不会完整写拉回参数平面的桥。
- 已教学并理解：
  \[
  d\mathbf r=r_u\,du+r_v\,dv
  \]
  \[
  A(u,v)=F(r(u,v))\cdot r_u,\quad
  B(u,v)=F(r(u,v))\cdot r_v
  \]
  \[
  \oint_{\partial S}F\cdot dr
  =
  \oint_{\partial D}A\,du+B\,dv.
  \]
- 用户一度把
  \[
  (D F\,r_u)\cdot r_v-(D F\,r_v)\cdot r_u
  \]
  看成会相消。
- 后续已深入教学：
  一般 Jacobian \(D F\) 不对称；
  \[
  \frac{DF-DF^T}{2}
  \]
  是反对称部分；
  三维反对称矩阵可由向量 \(\omega\) 编码：
  \[
  Kh=\omega\times h;
  \]
  且
  \[
  \omega=\frac12\nabla\times F.
  \]
- 用户已理解：
  curl 是 \(DF\) 反对称部分的三维向量编码（注意对象类型不同，不能直接说向量等于矩阵）。
- 已补完核心恒等式：
  \[
  B_u-A_v
  =
  (\nabla\times F)\cdot(r_u\times r_v).
  \]
- 已理解分片时内部边界抵消与 Green 证明是同一局部到整体机制。
- 该部分目前是 conceptual reconstruction completed / independent transfer pending，不得直接写 mastered。

B5：
- 主路线正确：
  隐式曲面→法向→选投影→dS。
- 原作答主要错在面积伸缩因子计算。
- 不升级为结构错误；后续只需轻量变式确认。

B6：
- 主要错在法向方向符号：题目要求 x 分量为负，用户取了相反方向。
- 属于方向执行控制问题；不宜扩大成“不会第二类曲面积分”。

B7：
题目：
\[
\oint_L y^2dx+z^2dy+x^2dz
\]
沿三角形 A→B→C→A。

用户正确完成了：
- 识别
  \[
  F=(y^2,z^2,x^2);
  \]
- 求
  \[
  \nabla\times F=(-2z,-2x,-2y);
  \]
- 选三点张成的平面
  \[
  6x+3y+2z=6;
  \]
- 用
  \[
  \overrightarrow{AB}\times\overrightarrow{AC}=(6,3,2)
  \]
  匹配边界方向；
- 理解 Stokes 后还要继续做曲面积分。

原来认为用户把点乘后的线性函数当常数，是“执行链断点”；用户随后明确澄清：
这是一次变量看串，把 x 看成 z，因此误以为可以借曲面方程化为常数。
所以：
- 不登记为 Stokes 结构错误；
- 不登记为长期 misconception；
- 但本题的计算方法非常有训练价值，用户明确要求“保留”。

请在后续题中保留这一类结构：
1. Stokes 后
   \[
   (\nabla\times F)\cdot n
   \]
   为非常数线性函数；
2. 积分面为三角形平面；
3. 可以利用“三角形上线性函数的面积平均 = 三个顶点值平均”或等价参数化完成；
4. 至少出 1 道，但不要简单换数字复刻；
5. 题面不要提示“线性函数平均值”。

这部分应登记为：
- Stokes main chain: positive evidence
- orientation matching: positive evidence
- post-dot-product surface integration: retain for reinforcement
- prior error cause: one-off variable misread, not structural

B8：
- 第一问开放曲面通量结果正确；
- 第二问正确识别内部奇点，知道不能直接 Gauss，能提出挖小球形成穿孔区域；
- 这是 Gauss singularity positive transfer evidence。
- 后续只需变化几何形状做一次条件迁移，不需要大量重复。

B9：
- 隐函数存在条件正确；
- 一般隐式偏导负号本轮正确；
- 路径复合求 w'(0), w''(0) 正确；
- 方向 (3,4) 先单位化后求方向导数正确。
- 上一轮两个断点“隐式偏导负号”“单位方向”都有正向迁移证据。

C10：
- Hessian 退化题正确；
- 能算
  \[
  H(0)=\mathrm{diag}(2,0)
  \]
  与核；
- 能主动配方
  \[
  f_a=(x+y^2)^2+(a-1)y^4+y^6
  \]
  并正确分类。
- 这是高阶退化主项与配方的独立正向迁移证据。

C11：
- 核心 Taylor 判断正确：
  \[
  \ln(1+u)-u+\frac12u^2
  =
  \frac13u^3+O(u^4)
  \]
- 能判断 \(u=O(\rho^2)\)，并得到极限主值。
- 主要问题是正式收束不够：
  未清楚写出最低次非零齐次多项式和精确余项阶。
- 更像“规范书写/主项+余项表达”问题，不宜升级为概念断点。

C12：
- Gauss 补面策略正确；
- 散度正确；
- 最终结果正确；
- 底面有向通量符号书写不够显式。
- 属于方向符号表达规范问题。

================================
二、当前需要重点保留的模型断点
================================

下一轮重点只保留真正有结构价值的内容：

1. 投影与参数化对象区分
   - R^2→R^3：局部面积用叉乘；
   - R^2→R^2：投影面积用 Jacobian determinant；
   - 能从
     \[
     \frac{\partial(y,z)}{\partial(u,v)}
     \]
     连接到
     \[
     (r_u\times r_v)_x=n_x||r_u\times r_v||.
     \]

2. Stokes 的参数平面拉回与旋度结构
   - \[
     F(r(u,v))
     \]
     的复合依赖；
   - \[
     A=F(r)\cdot r_u,\quad B=F(r)\cdot r_v;
     \]
   - \[
     B_u-A_v
     \]
     为什么只保留 \(DF\) 的反对称部分；
   - 一般 \(DF\) 不对称；
   - 三维反对称部分与 curl 的关系。
   这部分需要“理解题/证明题”，不是只出计算题。

3. 曲面积分/Stokes 后续积分
   - 点乘后若仍依赖位置变量，必须继续积分；
   - 保留一次“三角形上线性函数”的漂亮计算结构；
   - 但不要把上次变量看串记录为错误模型。

================================
三、下一轮建议题量与结构
================================

生成 9–11 题，建议：

A. 理解/证明 3题
1. 一题专门区分
   \[
   R^2\to R^2
   \]
   的 Jacobian 面积伸缩和
   \[
   R^2\to R^3
   \]
   的叉乘面积伸缩。
   要求用户解释对象类型，不只算式。

2. 一题要求从
   \[
   A=F(r)\cdot r_u,\quad B=F(r)\cdot r_v
   \]
   推到
   \[
   B_u-A_v=(curl F)\cdot(r_u\times r_v).
   \]
   可以允许矩阵反对称部分路线或分量展开路线，但不要题面提示哪一种。

3. 一题考
   \[
   DF=S+K
   \]
   对称/反对称分解，
   要求解释为什么 Stokes 只看反对称部分，以及三维为何能用一个向量编码 K。
   不要求超出考研范围的大理论，只训练本轮已教学理解。

B. 计算/迁移 4–5题
1. 一题隐式曲面第一类积分，投影面不要总选 xy。
2. 一题第二类曲面积分，重点方向和有向投影面积。
3. 一题 Stokes：点乘后得到三角形上的非恒定线性函数，保留“三角形平均值/参数化”计算价值。
4. 一题 Stokes：点乘后恰好可用曲面方程化成常数，和上一题形成对照，让用户判断“何时真的可化为常数”。
5. 一题 Gauss 含补面或奇点几何变化，轻量验证即可。

C. 高阶/综合 2题
1. 一题 Hessian 退化高阶主项，换结构，不复刻 Q10。
2. 一题 Taylor 主项 + 余项阶，要求明确写“最低次齐次项 + 余项阶”，针对 Q11 规范输出。

================================
四、出题要求
================================

- 难度比考研常规题高约 1 档；
- 难度来自对象转换、方向、条件、表示和方法选择，不靠大计算量；
- 不在标题中泄露“Stokes/Gauss/Jacobian/反对称”等方法标签；
- 不直接提示使用哪一个定理；
- 不简单换数字复刻 MV0916；
- 每题要求“结论 + 关键理由 + 必要计算”；
- 理解题要求写核心数学链条；
- 计算题允许合法不同路线；
- 保留 exposure_context；
- 刚教学过的内容只能算近期暴露后的迁移，不得作为冷启动独立发现证据。

================================
五、状态更新建议
================================

请把最新状态更新为类似：

- surface_parameterization:
  conceptual_reconstruction_completed
  execution_transfer_partial
  projection_jacobian_distinction_pending_transfer

- stokes:
  main_chain_positive_evidence
  parameter_pullback_conceptual_reconstruction_completed
  antisymmetric_jacobian_curl_model_established
  independent_transfer_pending

- surface_integral:
  projection_and_orientation_execution_partial
  simple_scale_error_not_promoted

- gauss_singularity:
  positive_transfer_evidence

- implicit_function_directional_derivative:
  positive_transfer_evidence

- degenerate_hessian:
  positive_transfer_evidence

- taylor_high_order:
  conceptual_and_computational_positive
  formal_main_term_remainder_writing_pending

特别注意：
Q7 的“把变量看串后误判常数”明确标为 one-off execution slip，不得升级成 misconception 或 user-active error model。

最终交付：
1. 更新后的学情文件；
2. 新题组学生卷；
3. 教师侧每题设计目标简表；
4. 不给学生答案。