# 多元微积分新一轮检测：当前学习状态与出题交接

版本：2026-09-15 当前教学状态增量。
用途：覆盖 2026-09-11 首次检测后的旧 `current_node` 判断，用于生成新一轮多元微积分检测题。
原则：以下“已教学/已建立模型”不等于已经独立验收通过。新题应检测无提示调用，而不是再次按首次教学方式直接提示。

## 1. 与旧报告的关系

保留 2026-09-11 首次 13 题检测作为历史基线，不删除原始错误证据。

但以下旧定位已经发生变化：

* `R1 固定基点线性化`：已完成深入教学，不再作为“尚未开始修复”处理。
* `R2 复合/隐函数`：已完成主要概念重建。
* `R3 梯度/方向导数`：已完成结构性重建。
* `R4 Hessian/极值`：已完成 Hessian、Taylor 与极值判别的深入重建。
* `R5 曲线/曲面参数化`：已开始并建立主要对象关系，但曲面投影与积分部分仍需继续闭环。
* Green/Gauss/Stokes 的条件与综合方法选择仍未完成当前轮重新验收。

因此，新卷不能简单复制第一次检测的结构，也不能把以下刚教学内容直接登记为“稳定掌握”。

---

## 2. 当前已经建立的统一模型

用户现在采用以下统一理解：

$$
\text{空间}
\to
\text{映射}
\to
\text{局部线性化}
\to
\text{参数化}
\to
\text{局部量累计}
\to
\text{整体关系}.
$$

### 2.1 点、向量与位移

在当前考研数学一欧氏空间语境下，允许直接把

$$
P\in\mathbb R^n
$$

作为位置向量处理。

若

$$
Q=P+h,
$$

则

$$
h=Q-P
$$

解释为输入空间中的位移向量。

用户已经明确建立：

$$
Df(P):\mathbb R^n\to\mathbb R^m
$$

不是“一个数”，而是局部线性映射。

Jacobian 是在选定输入/输出基后对该线性映射的矩阵表示；一元函数

$$
\mathbb R\to\mathbb R
$$

只是退化成 \(1\times1\) 矩阵，所以传统导数表现为一个数。

### 2.2 标量函数、线性泛函与梯度

对

$$
f:\mathbb R^n\to\mathbb R,
$$

用户已建立：

$$
Df(P):\mathbb R^n\to\mathbb R
$$

是线性泛函。

标准欧氏结构下：

$$
Df(P)[h]
=
\nabla f(P)\cdot h.
$$

用户已经区分：

* \(Df(P)\)：抽象线性泛函；
* \(\nabla f(P)\)：该泛函在欧氏内积下的向量表示；
* 标准矩阵表示为 \(1\times n\) 行向量，而梯度通常写作 \(n\times1\) 列向量。

### 2.3 基点变化方向与作用方向

这是本轮重点新建结构。

二阶导数

$$
D^2f(P)[k,h]
$$

中的两个变量不能混为一谈：

* \(k\)：基点 \(P\) 本身移动的方向；
* \(h\)：在固定基点后，一阶导数 \(Df(P)\) 的作用方向。

用户现在能用语言表述：

> 固定一阶作用方向 \(h\)，考察当基点沿 \(k\) 变化时，\(Df(P)[h]\) 如何变化。

并理解：

$$
D^2f(P)
$$

对 \(k,h\) 分别线性，即双线性；“双线性”不是同时把两个变量当成一个线性变量。

---

## 3. Hessian、二次型与 Taylor 的当前理解

### 3.1 Hessian

用户已理解 Hessian 是二阶导数这个双线性映射的矩阵表示。

若

$$
D^2f(P)[k,h]
$$

在标准基下表示，则

$$
D^2f(P)[k,h]
=
k^TH_f(P)h.
$$

令两个槽位取同一个位移：

$$
k=h,
$$

得到

$$
D^2f(P)[h,h]
=
h^TH_f(P)h,
$$

即 Hessian 对应的二次型。

用户已经理解“二次型”不是人为规定，而是双线性形式在两个输入取同一向量时自然产生。

### 3.2 Hessian 正定与极值

用户已经把多元微积分与线性代数中的正定理论接通：

对于实对称 Hessian \(H\)：

$$
H>0
\iff
\lambda_i>0
$$

并能联系：

* 顺序主子式；
* 正惯性指数；
* 正交对角化；
* 二次型。

通过

$$
H=Q\Lambda Q^T,\qquad z=Q^Th,
$$

得到

$$
h^THh
=
\sum_i\lambda_i z_i^2.
$$

若

$$
\lambda_{\min}>0,
$$

则

$$
h^THh
\ge
\lambda_{\min}\|h\|^2.
$$

用户已经理解二阶 Taylor 余项

$$
o(\|h\|^2)
$$

可以被压到比上述主项更小，因此余项无法翻转正定二次主项的符号。

已讲清：

* 正定 → 严格局部极小；
* 负定 → 严格局部极大；
* 不定 → 鞍点；
* 半正定/半负定 → 二阶判别一般不足，需要更高阶项。

### 3.3 Taylor 的核心模型

本轮最重要的新建模型如下。

Taylor 的目标不是研究某条真实运动路径，而是：

> 给定固定展开点 \(P\) 和附近任意点 \(Q=P+h\)，利用 \(P\) 点的局部信息近似 \(f(Q)\)。

为了把多元问题降为一元问题，人为选择最简单的连接路径：

$$
\gamma(t)=P+th,\qquad 0\le t\le1.
$$

用户已明确理解：

* 直线不是“代表所有可能路径”；
* Taylor 关心的是所有附近点，而不是到达该点的所有路径；
* 对每个任意附近点 \(Q\)，都可以用一条自己的直线连接 \(P,Q\)；
* 因为 \(h\) 本身任意，所以最终结论覆盖整个邻域；
* 选择直线是因为最简单，并且

  $$
  \gamma'(t)=h,\qquad \gamma''(t)=0.
  $$

定义：

$$
g(t)=f(P+th).
$$

则

$$
g'(t)
=
Df(P+th)[h].
$$

用户现在理解其中：

* 当前基点是 \(P+th\)，随 \(t\) 变化；
* 作用方向 \(h\) 在这条直线路径上保持固定；
* \(Df(P+th)\) 是当前点处的局部线性映射，而不是一直使用 \(Df(P)\)。

### 3.4 局部累计到整体

用户已经理解微积分基本定理在此处的角色：

$$
g(1)-g(0)
=
\int_0^1g'(t)\,dt.
$$

翻译为：

$$
f(P+h)-f(P)
=
\int_0^1Df(P+th)[h]\,dt.
$$

当前理解：

> 路径上每个点提供一个局部变化率；把“局部变化率 × 极小参数变化”累计，就得到起点到终点的总函数值变化。

进一步：

$$
g'(t)-g'(0)
=
\int_0^t g''(s)\,ds,
$$

代回以后得到：

$$
g(1)
=
g(0)+g'(0)
+
\int_0^1(1-t)g''(t)\,dt.
$$

翻译回多元：

$$
f(P+h)
=
f(P)
+
Df(P)[h]
+
\int_0^1
(1-t)
D^2f(P+th)[h,h]\,dt.
$$

用户已经理解：

* 这是精确等式；
* Taylor 的进一步目的，是把沿途局部信息逐层压缩成固定基点 \(P\) 处的各阶导数；
* 权重 \(1-t\) 可以理解为：越早产生的斜率变化会影响越长的后续区间。

三阶结构也已讲授：

$$
g(1)
=
g(0)+g'(0)+\frac12g''(0)
+
\frac12\int_0^1(1-t)^2g'''(t)\,dt.
$$

以及

$$
g'''(t)
=
D^3f(P+th)[h,h,h].
$$

用户理解阶乘来自反复积分，而不是人为规定。

### 3.5 Taylor 当前尚未独立验收

虽然概念教学已经深入完成，但以下仍属于“刚建立模型”，不得直接登记稳定掌握：

* 无提示从题目中判断是否应使用 Taylor；
* 独立选择展开点和所需阶数；
* 主项提取；
* 多元极限中的余项控制；
* 复合内部量的阶数判断；
* 积分余项的独立推导；
* Taylor 与 Hessian 极值判别的无提示连接；
* 固定路径的一元 Taylor 与完整多元 Taylor 的区分。

新检测应包含对此的独立调用，而不是再次告诉用户“用 Taylor”。

---

## 4. 参数化、曲线和曲面的当前状态

用户已经建立：

### 曲线

$$
\gamma:I\to\mathbb R^n.
$$

几何曲线是像集，参数化额外记录方向、顺序和速度。

$$
d\mathbf r=\gamma'(t)dt,
$$

$$
ds=\|\gamma'(t)\|dt.
$$

用户理解：

* \(d\mathbf r\)：有向小位移；
* \(ds\)：其长度；
* 反向后 \(d\mathbf r\) 变号，而 \(ds\) 不变。

### 曲面

$$
r(u,v):D\subset\mathbb R^2\to\mathbb R^3.
$$

已建立：

$$
r_u,\quad r_v
$$

是两个参数方向对应的切向量，

$$
d\mathbf S
=
(r_u\times r_v)dudv,
$$

$$
dS
=
\|r_u\times r_v\|dudv.
$$

用户已经理解叉乘出现的原因：

* \(r_u du,r_v dv\) 张成局部平行四边形；
* 叉乘模长给面积；
* 叉乘方向给法向；
* 更本质上这是二维参数面积经 \(Dr\) 后的面积伸缩。

但以下尚未在本轮真正闭环：

$$
dS,\quad dxdy,\quad |\cos\gamma|,\quad n\,dS
$$

之间的统一关系，以及它们在第一/第二类曲面积分中的独立调用。

因此旧报告第10、12题对应内容仍应作为重要检测目标，但需要换结构，不应原题换数字。

---

## 5. 新一轮检测应该测什么

新卷的目的不是重新诊断“整个多元微积分有没有学过”，而是：

> 检验本轮重建后的模型能否在无决定性提示条件下独立调用，同时复查旧报告中仍未闭环的积分几何与定理条件。

建议仍保持约 8 题，不做 13 题大卷。

### A. 多元微分 / Taylor：3题

至少覆盖：

1. **固定基点与导数线性映射**

   * 不直接问定义；
   * 给一个需要判断可微、主线性项或余项阶数的题；
   * 必须迫使用户区分 \(Df(P)\) 与邻近点偏导。

2. **Taylor/Hessian 综合**

   * 不在题面提示“使用 Taylor”；
   * 要求判断局部行为、极限或极值；
   * 最好需要用户自行识别一阶消失 → 二阶主项；
   * 不应只是机械套二维判别式。

3. **Taylor 路径/积分余项或复合结构**

   * 可测试

     $$
     g(t)=f(P+th)
     $$

     的独立构造；
   * 或给一个内部量本身已经是二阶小量的表达式，检测所需展开阶数；
   * 题目不得直接给出路径构造步骤。

### B. 复合/隐函数/方向：1题

综合：

* 复合依赖；
* 隐函数存在条件；
* 沿给定路径求一阶/二阶变化；
* 必要时包括单位方向问题。

不要复刻旧第2题。

### C. 曲线/曲面参数化与积分几何：2题

一题重点区分：

$$
d\mathbf r,\quad ds
$$

或第一/第二类曲线积分的方向性质。

另一题重点检验：

$$
dS,\quad d\mathbf S,\quad dxdy,\quad n
$$

以及投影尺度关系。

旧报告第10、12题对应漏洞必须在改变表面结构后重新检测。

### D. Green/Gauss/Stokes 条件与方法选择：2题

至少一题必须具有：

* 公式形式看起来可以直接使用；
* 但存在决定性条件需要先检查，例如奇点、闭合性、方向、补面或边界一致性；
* 正确作答必须明确说明“能不能用以及为什么”。

另一题做混合选法：

* 不提示 Green/Gauss/Stokes 名称；
* 允许直接积分和定理路线存在明显计算差异；
* 用户需要自行决定较自然的方法并检查条件。

---

## 6. 出题难度与结构要求

用户目标为 2027 考研数学一，有较强旧基础。

新题可以比正式考研常规题难约 1 档，但难度应来自：

* 多一步结构判断；
* 条件改变；
* 对象角色区分；
* 表示转换；
* 方法链组合。

不要靠：

* 超纲定理；
* 大量机械计算；
* 极长积分；
* 生僻背景；
* 单纯换数字增加运算量。

题目不得在标题中写：

* “Taylor题”
* “Green公式题”
* “Hessian判别题”
* “投影面积题”

否则会破坏无提示方法选择证据。

每题要求：

$$
\boxed{\text{结论 + 关键依据 + 必要计算}}
$$

不要求长篇证明，除非题目本身明确测证明能力。

---

## 7. 证据解释规则

本轮刚教学过的 Taylor/Hessian/路径模型即使答对，也只能算近期教学后的独立调用证据，不自动登记长期掌握。

如果用户答错：

* 先定位第一处实质断点；
* 不把后续连锁错误重复计数；
* 不因单题失误升级为“整个模块不会”。

特别注意区分：

* 概念模型正确但执行算错；
* 方法选择错误；
* 条件遗漏；
* 对象角色混淆；
* 表示转换错误。

旧报告中的单次算术失误（如积分执行、半球体积）不得无证据升级为稳定错误模型。

---

## 8. 建议题组结构

建议 8 题：

$$
\boxed{
3\text{ 道微分/Taylor}
+
1\text{ 道复合隐函数}
+
2\text{ 道曲线曲面}
+
2\text{ 道积分定理/混合选法}
}
$$

其中至少：

* 2题为 R1 无提示独立调用；
* 2题为 T1 条件判别；
* 1题为 T2 表示迁移；
* 1题为 T3 公式失效边界；
* 其余可用于综合执行。

不要把刚讲过的例子：

$$
e^{x+y},\quad e^{x^2+y^2},\quad x^2-y^2
$$

直接换数字复用。

必须换表面形式。

---

## 9. 当前最需要新证据的问题

优先级从高到低：

1. Taylor 是否能从“理解”转成无提示调用；
2. \(Df(P)\)、\(D^2f(P)[k,h]\)、Hessian 二次型的对象角色是否在新题中保持稳定；
3. 曲面实际面积、投影面积、有向面积是否真正分开；
4. Green/Gauss/Stokes 是否会先检查合法性条件；
5. 混合题中能否自己选择表示和方法；
6. 旧的计算执行失误是否仍然复发。

新卷完成后，再依据真实作答决定当前路线节点，不在出题前提前登记 R1–R4 已通过。
