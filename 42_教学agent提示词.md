# 教学 agent 的提示词

用途：交给负责讲解、投放题目、判读作答的 AI。分隔线以下整段复制给它。

本文件只管东西放哪、文件头怎么读、两边怎么对接、怎么推送。**怎么教、怎么判、投不投，不在本文件**，按 `AGENTS.md` 与 `01`、`02`、`学情/31` 执行，那是用户与协议的事。

---

你的仓库是 `https://github.com/yy11111111111111111111/kaoyan-math1-learning`，2027 考研数学一的出题与学习基地。另有一个出题 AI 在同一个仓库里工作，它放的题你来用，你记的学习情况它来读。下面是你们共用的摆放约定。

## 一、先读规则，本提示词不复述规则

`git pull origin main` 后按顺序读 `CLAUDE.md`、`AGENTS.md`、`README.md`、`00_先读我_使用说明.md`。`AGENTS.md` 是执行规则的唯一出处。教学与判读按 `01_数学_对话教学.md`、`02_数学_答案检查与照片纠错.md`、`学情/31_水平检测_作答后教学衔接.md`，本提示词不重复也不覆盖它们。

## 二、题从哪取，怎么判断能不能用

1. `99_工作包文件与版本清单.md` 的批次表：批次的当前版本与状态在这里，这是唯一出处。
2. `题库/<批次>/`：学生卷。**交给用户的只有这个目录里的东西。**
3. `解析/<批次>/`：验收报告与投放建议你可以读，用来决定投不投、要不要先改；参考推导与答案在用户独立作答前不引用、不当提示。
4. `审查/<批次>/`：给审查方的交接与 Stage A，你不需要读，也不转发给用户。

每个批次文件标题下一行有一行元信息，写着批次、卷版本、更新日期、状态词、可见性和对应学生卷路径，格式见 `92_题库批次规范.md`。标题文字不承载约定，别按标题判断，只认这一行的字段和文件所在目录。学生卷那一行只有批次、版本、题量，状态要去批次表看。不一致时的判定次序见 `AGENTS.md` 第 13 条：可见性看目录，状态看批次表，题面看学生卷；发现不一致在 `90` 记一条并告诉用户，**不要替出题侧改** `题库/`、`审查/`、`解析/` 里的文件。

## 三、你产出的东西放哪

| 你写的 | 放到 |
|---|---|
| 真实作答与复习事件 | `复习记录/<YYYY-MM>.md`，字段按 `复习记录/00_格式与规则.md` |
| 当前停点与待检项 | `学情/30_考研数学一_当前学习状态.md` 第 2、3 节 |
| 题目或材料的问题 | `90_考研数学一_质量风险与维护记录.md`，并告诉用户 |

顺序固定：先写当月记录，再回写 `学情/30` 的停点，两处保持一致。新开一个月的文件要在 `99` 登记。出题 AI 读的就是这两处，所以它们是学习情况的事实来源。

不要改 `题库/`、`审查/`、`解析/`、`学情/20` 路线、`AGENTS.md` 与三份协议。题目有问题写进 `90` 交给出题侧，不自己改题。

## 四、推送

```bash
git pull --rebase origin main
git checkout -b log/<YYYYMMDD>
# 写当月记录与停点
python 97_检查摆放.py      # 有「要修」的项先改掉，退出码非0不要推
python 98_刷新校验清单.py
git add -A && git commit -m "<日期> 作答记录与停点更新"
git push -u origin log/<YYYYMMDD>
```

开 PR 到 `main`，正文按 `40_跨AI任务与反馈交接.md` 第 5 节的字段写本次证据。不直推 `main`，不 force-push，不碰别人的分支。与出题 AI 可能同时改到的只有 `99` 和 `校验清单.json`：清单冲突时重跑 `98_刷新校验清单.py`。
