# 出题协议修订记录

本文件是维护记录，不是第三套运行协议，不加入普通教学默认加载链。

## 当前发布

- Generation：`数学题_Generation_Spec.md`，1.1.0。
- Structural Validation：`数学题_Validation_Protocol.md`，1.1.0；报告ID为`MATH-ITEM-VALIDATION-v1.1.0`。
- 共享声明集：仍为`MATH-ITEM-DECL-v1.0.0`；`TASK-FP-v1.0`、`ROUTE-SEARCH-v1.0`及全部claim最低等级保持原值。
- 日期：2026-09-06。文件直接修改于主目录，全局目录链接读取同一份文件；没有建立新版子文件夹。

## 授权与依据

用户先要求按实际情况优化Generation与Validation，在看到具体补丁和研究档缺失说明后明确回复“允许不等待”。本轮据实际读取的启动入口4.0、03模块3.0、共享声明集1.0.0、两份协议1.0.0及用户最新教学规则修订。没有读取缺失的研究依据版，不声称本次是研究档一致性验收；豁免不自动适用于未来维护。

## 实际修改

| 问题 | 修订 |
|---|---|
| 缺少对话小题与阶段题组的执行边界 | 增加生成侧delivery_mode、route_binding；一次一题为默认，明确题组请求可整组交付；不机械每讲必出一套 |
| Generation与Validation交接字段不一致 | 统一Stage A的required_prerequisites、convention_bindings等字段；统一Stage B的答案集合、建议等级及呈现证据；同步40交接模板 |
| 评分字段或引用目标可能预先暴露答案 | 分开评价准则与答案键，检查内容与引用目标；不删除Q的必要条件来伪造盲验；题面要求证明的结论不自动算泄露 |
| 生成者复算可能被冒称为Light独立验证 | 明确所有等级Core均需初始答案盲；缺执行条件如实记录，不降低Full或伪造独立上下文 |
| Light文字要求把已执行Extended也写成skipped | 改为按真实执行状态记录，未要求项不为填表强制执行 |
| 无提示结论未完整绑定真实呈现 | 新增presentation_evidence与K_leakage，纳入提示、题序、前题解答可见性；缺背景不默认无提示 |
| 工具或呈现变化后的缓存依据不明确 | 新增变更传播行和leakage重验对象；旧报告不自动升级，保留仍有效的独立求解证据 |
| 预设路线或模板措辞可能误判合法响应 | 接受公开约束下的合法替代解法；不认领未展示能力；只换数字可执行巩固但不算迁移 |

同步更新使用说明、全局入口、版本清单、风险记录及校验清单中的当前事实。旧“优化补丁草案”由本记录取代；前轮共享声明集的独立建议没有自动应用，原草案保留于恢复备份。

## 已完成核对

1. Generation、Validation与40交接模板的Stage A顶层字段逐项一致；Stage B顶层字段逐项一致。
2. Validation的八个claim requirement表行与1.0.0逐字一致，没有降低Light/Full下限或证据要求。
3. 报告协议ID已更新；共享声明集、TASK-FP和ROUTE-SEARCH版本没有随文档编号误升。
4. 主目录与全局入口读取的两份正式协议哈希一致；Markdown代码块成对。
5. 启动入口、03、共享声明集、路线、学习状态及两份方法族均与本轮修改前哈希一致。

语义走查：只要求解释时不强插题；对话小题不因题小自动降级；答案已暴露时不得冒充独立验收；合法不同解法不因偏离生成路线扣分；前文新增公式时重验呈现依赖；明确要求题组时逐题验收并检查顺序提示。这是文档流程走查，不是实际题目盲验、软件集成测试或教学效果测量。

## 剩余边界

- 研究依据版仍缺失。本轮授权已满足此次维护需要，但不能声称已读研究档。
- 03§5第8项仍把只换数字无条件列为失败；03§2.1和共享§4.1的`x not in s_u`尚未显式区分未知与失败。按用户最新规则及Generation 1.1.0处理，见90的R12；这些文件未在本轮静默改写。
- 本次没有生成数学题、建立真实独立验收样本、更新学生状态、修改题库或推送GitHub。正式协议已发布，不等于其教学效果已经验证。
- 独立Validator调用条件不存在时，当前协议仍只能交付待验收材料；本次没有用文档修订伪造该执行能力。

## 恢复位置

修改前的文件与原草案保存在`C:/Users/欧阳佳杰/.codex/config-backups/math-protocol-v110-20260906`，不参与教学读取。旧报告保留其原协议版本；后续重验按依赖补做，不回写成“当时已经完成新检查”。
