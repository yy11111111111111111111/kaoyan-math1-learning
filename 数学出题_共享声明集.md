# 数学出题共享声明集

> 版本：1.0.0  
> 声明集版本：`MATH-ITEM-DECL-v1.0.0`  
> 职责：Generation、Structural Validation 与 Assignment Fit 共用的唯一声明源。  
> 本文件只登记稳定对象、封闭枚举和待裁定声明；不保存单题报告、学生作答或整门课程知识副本。

## 1. 总体边界

数学题协议采用：

```text
Generation
→ Structural Validation
→ Population Calibration（可选；无真实群体数据时为 uncalibrated）
→ Assignment Fit
```

反馈回边采用：

```text
counter-witness
→ challenged
→ quarantine
→ Targeted Revalidation
```

三个结论严格分离：

```text
题目在某 claim 下结构合法
≠ 当前适合投放给某学生
≠ 该学生已经掌握相应能力
```

Generation 可以读取学生行为状态 $s_u$；Structural Validation 不得读取 $s_u$；Assignment Fit 在题目完成相应验收后才读取 $s_u$。

## 2. 四条全局证据纪律

```text
未找到 ≠ 证明不存在
没有行为证据 ≠ 证明学生不会
学生答错 ≠ 证明题目有错
缺少判据 ≠ 已有判据但证据不足
```

最后一条等价于：

```text
undecidable ≠ not_established
```

任何模块都不得把左侧静默升级为右侧的强结论。

## 3. Registry 的封闭性与升级

本文件中的 $P$、$C$、$\mathcal K$、$\mathcal M$、validation level 和 route-search protocol 均为版本化 registry。

1. Validator 只能使用当前版本已经登记的声明。
2. 单题失败后不得临时发明 claim、约定、前置或错误模型，使题目重新“通过”。
3. 找不到判据时输出 `undecidable`，把一条 `status: open` 记录写入本文件的 `pending_declarations`，停止受影响检查；该写入只登记缺口，不补判据。
4. 人工裁定后升级本文件版本；旧报告保留其原声明集版本，不追溯改写。
5. 仅措辞澄清且不改变判据可提升 patch 版本；增加对象或改变判据至少提升 minor 版本；破坏兼容性时提升 major 版本。

## 4. 允许前置集合 $P$

### 4.1 定义

允许前置集合 $P$ 是：

> 某题在声明用途下，可以公平要求学习者已经拥有的定义、定理、表示、程序和基本技能。

必须保持：

$$
P\ne s_u.
$$

允许出现：

$$
x\in P,\qquad x\notin s_u.
$$

此时题目可能结构合法，但 Assignment Fit 必须把当前学生的前置缺口列为不适配证据。Validator 不得因为学生暂时不会就缩小 $P$，也不得因为课程已经讲过就推定 $x\in s_u$。

### 4.2 Profile registry

| profile_id | 引用来源 | 允许范围规则 |
|---|---|---|
| `P-MATH1-COMMON-v1` | `00_考研数学一_总入口.md`；当前任务指定的官方大纲、教材或题面 | 只允许来源明确覆盖且在单题声明中列出的通用定义、表示和基本运算；目录出现不自动进入 $P$ |
| `P-MATH1-CALC-v1` | `01_高等数学_总入口.md`；当前高数资料入口 | 只允许当前 claim 明确引用的小节范围及其已声明直接前置 |
| `P-MATH1-LA-v1` | `02_线性代数_总入口.md`；当前线代资料入口 | 只允许当前 claim 明确引用的小节范围及其已声明直接前置 |
| `P-MATH1-PROB-v1` | `00_概率统计_总入口.md`；`04_概率论_教学接口.md`；当前指定教材 | 同时声明对象层级、模型假设、支持集和精确/近似边界；不得只凭公式名扩大范围 |

Profile 只提供引用入口，不把课程知识复制到本文件。每个 item package 仍须列出本题真正调用的 `required_prerequisites` 及其来源位置。

### 4.3 单题绑定格式

```yaml
prerequisite_profile: P-MATH1-LA-v1
required_prerequisites:
  - id_or_statement: 主元列与线性无关组的对应关系
    source_ref: 02_线性代数_总入口.md#...
  - id_or_statement: 初等行变换保持方程组同解
    source_ref: 当前教材或已声明定义
scope_rule: 只允许上述节点及其显式直接前置
```

若必要前置不在题面、$P$ 或 $C$ 中，且题目也没有明确要求学习者把它作为待补条件，则 `prerequisite_scope_P` 不能得到支持。

## 5. Convention 集合 $C$

### 5.1 使用规则

Convention 只能来自已经登记的项目约定、指定来源约定或完整 item package。禁止使用“通常考试都默认如此”补条件。

若条件不在题面、不在当前绑定的 $C$，也不是题目明确要求补出的对象，则 Validator 不得自行补齐。

### 5.2 Convention registry v1

| convention_id | 约定 | 适用边界 |
|---|---|---|
| `C-ITEM-EXPLICIT-v1` | 完整 item package 中显式写出的定义域、参数范围、模型假设、记号和作答规则均可使用 | 只对当前 item package 有效；标题、选项、instructions、response requirement 与 scoring rule 都属于 package |
| `C-SOURCE-LOCAL-v1` | 指定教材、讲义、官方大纲或项目入口中的局部约定，只有在 item package 给出精确来源引用后才可使用 | 不得从同一来源的其他章节类推；来源冲突时必须声明采用哪一口径 |
| `C-PROB-RANDOM-SAMPLE-IID-v1` | 在 `P-MATH1-PROB-v1` 下，只有当前指定来源已把“随机样本”定义为来自同一总体的 i.i.d. 随机变量时，题面可引用该术语而不重复展开 i.i.d. | 必须绑定定义来源；其他抽样设计、相关样本或不放回抽样不适用 |
| `C-USER-SUPPLIED-v1` | 用户在当前任务中明确提供且未与高优先级来源冲突的约定可以绑定到当前 item | 仅对当前任务有效，不自动升级为项目全局约定 |

未经登记的默认定义域、参数化或考试习惯不能作为隐含条件。

## 6. 封闭 measurement claim 集合 $\mathcal K$

所有 claim 均描述 item eligibility，不描述学生已经具备的能力。

| claim_id | 标准措辞 | 结构性含义 |
|---|---|---|
| `K-EXEC` | `eligible_for_known_method_execution` | 在方法或程序已给定/已限定的前提下，可用于获得合法执行证据 |
| `K-RECON` | `eligible_for_local_reconstruction` | 可用于撤去一个已编码的局部关系或关键动作并要求重建 |
| `K-SELECT` | `eligible_for_unprompted_method_selection` | 题面未泄露决定性方法，可用于无提示候选生成、选择及条件核验 |
| `K-COND` | `eligible_for_condition_discrimination` | 可用于判断决定合法性、分支、优先级或结论的条件变化 |
| `K-REP` | `eligible_for_representation_transfer` | 可用于在改变表示后识别并保持目标数学结构 |
| `K-BOUND` | `eligible_for_boundary_or_failure_identification` | 可用于识别边界、反例、失效点或结论不能推出的位置 |
| `K-COMP` | `eligible_for_condition_completion` | 信息不足是被设计的目标，学习者需给出缺失条件或说明不可判定 |
| `K-DIAG` | `eligible_for_declared_model_partition_separation` | 可用于区分单题声明的局部错误模型分区 $\mathcal M_q$ |

`K-DIAG` 默认只支持局部 partition，不支持“区分所有可能错误模型”。没有注册的 claim 必须输出 `undecidable`，不得临场改名后继续。

## 7. Claim 五态

Structural Validation 对每个目标 claim 只能输出：

| status | 严格定义 |
|---|---|
| `supported` | 该 claim 的全部必需检查均已执行，且达到 requirement table 规定的证据强度 |
| `contradicted` | 找到直接反驳该 claim 的具体 counter-witness |
| `not_established` | 判据存在且相关检查已经执行，但证据不足，或某个已知必需检查未达到要求 |
| `not_assessed` | 当前 validation level 没有执行该项，且该项不是当前 claim 的必需检查 |
| `undecidable` | 当前声明集没有足够判据，无法定义或完成该判断 |

`undecidable` 必须同时记录：

```yaml
status: undecidable
missing_declaration: 缺少的 P / C / K / M / level / protocol 判据
```

禁止：

```text
undecidable → 猜测 → supported
```

## 8. 纸面证据强度

| evidence_strength | 含义 | 允许的表述 |
|---|---|---|
| `proved` | 有正式论证、穷尽证明或与声明范围相称的完备证明 | 可以在证明覆盖的精确范围内作全称/不存在主张 |
| `witnessed` | 找到具体存在性证据 | 只能证明“至少存在一个”，如第二解、leakage、feature、counterexample |
| `verified_within_declared_scope` | 在事先声明且实际执行的有限范围内得到支持 | 必须附 scope、budget 和 protocol version；不得外推到范围之外 |
| `not_found` | 在声明的范围和预算内没有找到 | 只能写“本次未找到”；不能写“不存在、无泄露、唯一、没有其他路线” |

每个 `not_found` 必须保存：

```yaml
search_scope: ...
budget_used: ...
protocol_version: ...
```

## 9. 错误模型集合 $\mathcal M$

### 9.1 三种状态对象

```text
M_library       形式化的竞争求解程序库；不代表当前用户具有该错误
M_candidate     当前一次作答的一种可检验解释
M_user-active   有额外行为证据支持、当前可用于投放和复发审查的用户模型
```

现有 Error Log、`00_关键误解与迁移记录.md` 和学习状态记录属于行为来源与证据记录；其中的自然语言条目不会自动成为正式 $\mathcal M$。只有完成下列 operationalization 后才能进入 registry。

### 9.2 Operationalized model 的最低格式

```yaml
model_id: M-LIB-...
version: 1
scope: 可运行的对象与任务范围
trigger: 何种题面或中间状态触发
action: 逐步执行的错误规则
termination: 在何种状态停止并给出响应
predicted_observable: 作答中可核对的输出、遗漏或第一断点
provenance: observed | literature | analytic_competitor
user_status: library_only | candidate | user_active | dormant
```

“粗心”“不会边界”“MLE 不熟”等不能机械运行，不得登记为正式模型。

### 9.3 Candidate → user-active

一次 fit event 只能产生 `M_candidate`。Candidate 必须同时满足：

1. trigger、action、termination 与 predicted observable 可写清；
2. 能机械复现当前作答的第一独立断点；
3. 不是已有模型的同义改写；
4. 不补入用户没有展示的隐藏步骤。

进入 `M_user-active` 至少还需要一项额外行为证据：

- 在独立的新任务或改变关键条件的 discrimination probe 中再次产生该模型预言；或
- 当前候选与既有、可追溯的复发/高信心/同规则多错记录完成逐项匹配。

单次高信心错误可以提高 candidate 的复核优先级，但不能单独完成激活。持久化仍使用现有 Error Log/学习状态流程，不建立第二套用户状态文件。

### 9.4 Library registry v1

```yaml
- model_id: M-LIB-COND-OMIT-v1
  version: 1
  scope: 结论或程序具有显式适用条件的任务
  trigger: 题面出现熟悉结论、公式或程序，同时至少一个适用条件需要核验
  action: 识别到公式/程序后直接执行，不检查控制合法性、分支或结论的条件
  termination: 得到形式结果后立即停止
  predicted_observable: 作答给出未限定结论，并省略决定性条件或在条件不满足时仍沿用原程序
  provenance: analytic_competitor
  user_status: library_only
- model_id: M-LIB-COND-INVERT-v1
  version: 1
  scope: 两个互斥条件控制不同分支、方向或结论的任务
  trigger: 学习者识别到需要分条件，但必须把每个条件映射到相应分支
  action: 写出控制条件后，把两个条件对应的分支、方向或结论互换
  termination: 完成被互换后的分支并给出结论
  predicted_observable: 作答显式提到决定性条件，但条件—分支映射方向稳定相反
  provenance: analytic_competitor
  user_status: library_only
```

该模型只用于显式声明的局部诊断，不表示任何用户当前持有它。

### 9.5 Diagnostic 的局部 $\mathcal M_q$

每道 `K-DIAG` 题必须从已登记的 library、candidate 或 user-active model 中声明有限子集：

```yaml
diagnostic_partition:
  side_A: [M-LIB-COND-OMIT-v1]
  side_B: [M-LIB-COND-INVERT-v1]
```

Validator 只验收该 partition。新增与 $\mathcal M_q$ 无关的模型不使原 separability 结论失效。只有显式 universal coverage claim 才依赖整个 $\mathcal M$；v1 不登记任何 universal diagnostic claim。

## 10. Validation level registry

### 10.1 Claim 最低等级

| claim_id | $L_{\min}(k)$ |
|---|---|
| `K-EXEC` | `Light` |
| `K-RECON` | `Light` |
| `K-COND` | `Light` |
| `K-SELECT` | `Full` |
| `K-REP` | `Full` |
| `K-BOUND` | `Full` |
| `K-COMP` | `Full` |
| `K-DIAG` | `Full` |

### 10.2 Item 风险等级

`L_risk(q)=Full` 的触发器包括：

- 选择题、多答案评分或自然语言解释可能影响唯一性；
- 目标包含“不存在、唯一、无泄露、最小、最优”等 negative/minimality 主张；
- 使用 Error Log、candidate 或 user-active model 形成诊断用途；
- 题面、标题、选项或上下文可能泄露方法；
- 使用机器转写、OCR、来源冲突或尚未登记的局部约定；
- 任务结果将用于高成本状态更新、正式诊断或跨模块掌握结论；
- Generation 或 Core 发现第二解释、边界反例、路线竞争或其他 challenge 信号。

其他普通、低风险执行或局部重建题可记 `Light`。最终等级为：

$$
L(q,k)=\max\left(L_{\min}(k),L_{risk}(q)\right).
$$

Risk 只能升级，不能降级 claim 的最低等级。

## 11. Route-search protocol registry

当前版本：`ROUTE-SEARCH-v1.0`。

### 11.1 Family registry

```text
F1 direct_definition
F2 equivalent_representation_or_transform
F3 structural_theorem_or_invariant
F4 case_or_boundary_analysis
F5 constructive_or_counterexample
F6 algebraic_or_computational_reduction
```

### 11.2 执行与预算

Full route search 必须在看 generator rationale 前先声明适用 families，然后记录：

```yaml
protocol_version: ROUTE-SEARCH-v1.0
families_declared: [F1, F2, ...]
families_scanned: [F1, F2, ...]
routes_verified: [route_id, ...]
budget_used:
  families_scanned: n
  candidate_routes_considered: n
  routes_fully_verified: n
stopping_reason: all_declared_scanned | counter_witness_found | budget_reached | claim_decided
minimality: proved | not_claimed
```

默认预算上限为：6 个 family、每个 family 至多 2 个实质不同候选、至多 4 条完整核验路线。预算是工程停止条件，不证明搜索穷尽；除非另有正式证明，`minimality: not_claimed`。

只有相同 protocol version 的 route profile 才能比较搜索规范是否近似一致。

## 12. `pending_declarations`

### 12.1 记录格式

```yaml
pending_id: PD-...
source_task: ...
missing_declaration: ...
affected_registry: P | C | K | M | validation_level | route_protocol
blocked_claim_or_check: ...
status: open | resolved | superseded
resolution: 未裁定时为空；裁定后写入新版本位置
```

### 12.2 当前条目

```yaml
- pending_id: PD-C-DEFAULT-DOMAIN-001
  source_task: 数学题生成—结构验收协议 v1 落地
  missing_declaration: 中国考试语境中，题面未声明变量所属数域时能否默认实数域
  affected_registry: C
  blocked_claim_or_check: condition_sufficiency for items with an unstated domain
  status: open
  resolution: 未裁定；在解决前，所有受影响 item 必须在题面或绑定来源中显式声明数域
```

Validator 遇到其他缺口时必须按本节格式追加 open pending；若当前运行没有写权限，必须输出可原样追加的完整记录并保持阻塞，交由声明集维护者落盘。Validator 不得自行填写 resolution、补规则或继续作出 `supported`。
